name: UnityBuild

on:
  workflow_dispatch:  # 👉 수동 실행 허용

permissions: write-all
jobs:
  buildForAndroid:
    name: Build Android🛸🛸🛸
    runs-on: windows-latest
    steps:
      - name: Check Out
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Cache Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-${{ hashFiles('ColorMemory/Assets/**', 'ColorMemory/Packages/**', 'ColorMemory/ProjectSettings/**') }}
          restore-keys: |
            Library-

      - name: Free Disk Space for Android
        run: |
          # Docker 이미지 삭제
          $dockerImages = docker images -q
          if ($dockerImages) {
            docker rmi $dockerImages
          }
          Get-PSDrive -PSProvider FileSystem | Where-Object {$_.Used -gt 0} # 디스크 공간 확인

      - name: 🧪 Decode and Validate Keystore File
        run: |
          # Base64 디코딩 후 파일 생성
          [System.IO.File]::WriteAllBytes("temp.keystore", [Convert]::FromBase64String("${{ secrets.ANDROID_KEYSTORE_BASE64 }}"))
          if (-Not (Test-Path "temp.keystore")) {
            Write-Host "❌ Failed to create keystore file from base64."
            exit 1
          }

          # keytool로 유효성 검사
          & "keytool" -list -keystore temp.keystore -storepass ${{ secrets.ANDROID_KEYSTORE_PASS }} > $null 2>&1
          if ($LASTEXITCODE -ne 0) {
            Write-Host "❌ Keystore file is invalid or password is incorrect."
            exit 1
          }

          Write-Host "✅ Keystore file is valid."

      - name: 🧹 Cleanup temp keystore
        if: always()
        run: |
          # temp.keystore 파일 존재 여부 확인 후 삭제
          if (Test-Path "temp.keystore") {
            Remove-Item -Force "temp.keystore"
          }

      - name: Unity Build
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          targetPlatform: Android
          projectPath: ./ColorMemory
          androidKeystoreName: user.keystore # This file won't exist, but this property needs to exist.
          androidKeystoreBase64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          androidKeystorePass: ${{ secrets.ANDROID_KEYSTORE_PASS }}
          androidKeyaliasName: ${{ secrets.ANDROID_KEY_ALIAS }}
          androidKeyaliasPass: ${{ secrets.ANDROID_KEY_PASS }}
          androidTargetSdkVersion: AndroidApiLevel34

      - name: Upload Build File
        uses: actions/upload-artifact@v4
        with:
          name: Build-Android
          path: build
